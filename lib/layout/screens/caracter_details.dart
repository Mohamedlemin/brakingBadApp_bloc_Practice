import 'package:flutter/material.dart';

class caractersDetails_screen extends StatelessWidget {
  const caractersDetails_screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello'),
    );
  }
}